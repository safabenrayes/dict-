const bonnePhrase = "La cour de l’école est décorée avec des ballons multicolores et des ampoules électriques."; function playAudio() { const synth = window.speechSynthesis; const utterance = new SpeechSynthesisUtterance(bonnePhrase); utterance.lang = "fr-FR"; synth.speak(utterance); } function corriger() { const reponse = document.getElementById("reponse").value.trim(); const resultat = document.getElementById("resultat"); if (reponse.toLowerCase() === bonnePhrase.toLowerCase()) { resultat.innerHTML = "Bravo ! C'est correct."; resultat.style.color = "green"; } else { resultat.innerHTML = Incorrect.<br>Phrase attendue : <i>${bonnePhrase}</i>; resultat.style.color = "red"; } }
